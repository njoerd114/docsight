services:
  docsight:
    image: ghcr.io/itsdnns/docsight:latest
    build: .
    container_name: docsight
    restart: unless-stopped
    ports:
      - "8765:8765"
    volumes:
      - docsis_data:/data
    env_file:
      - path: .env
        required: false

volumes:
  docsis_data:
